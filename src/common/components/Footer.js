import React from 'react';

import { Title } from './styled/';
import SectionUI from './Section';


const Footer = (props) => {
    const { links } = props
    return (
        <SectionUI>
            <Title>
                porueba
            </Title>
        </SectionUI>
    )
}

export default Footer;